# -*- shell-script -*- #

if [ -f /etc/bash_completion ] && ! shopt -oq posix; then
    . /etc/bash_completion
fi

# I hate hate hate SVN.
complete -f -X '*.svn' ls cd less emacs pushd cat grep find 
